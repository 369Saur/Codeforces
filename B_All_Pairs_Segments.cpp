#include<bits/stdc++.h>
using namespace std ;
#define int long long

void solve(){
    int n , q ;
    cin >> n >> q ;
    vector<int> v(n) ;
    for(int i = 0 ; i < n ; i++) cin >> v[i] ;
    vector<int> que(q) ;
    for(int i = 0 ; i < q ; i++) cin >> que[i] ;
}

signed main(){
    int t ; cin >> t ;
    while(t--) solve() ;
    return 0 ;
}